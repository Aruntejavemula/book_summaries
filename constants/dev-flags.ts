/*
 * Dev/testing flags. Shared across screens so the login screen itself
 * also respects the bypass (not just the entry router).
 *
 * Turn both off before release.
 */
export const BYPASS_LOGIN = false;
export const FORCE_ONBOARDING = true;
